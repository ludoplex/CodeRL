interface:
  display_name: "CodeRL"
  short_description: "Repo-specific patterns and workflows for CodeRL"
  default_prompt: "Use the CodeRL repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true